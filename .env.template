# MKSAP Authentication Credentials
# Copy this file to .env and fill in your credentials

# Runtime (optional)
# MKSAP_PYTHON_VERSION=3.11.9

# Username (email address)
MKSAP_USERNAME=your-email@example.com

# Password
MKSAP_PASSWORD=your-password-here

# Session cookie (optional - auto-login will create if not provided)
MKSAP_SESSION=your-session-cookie-if-available

# Discovery settings (optional)
# MKSAP_REFRESH_DISCOVERY=1
# MKSAP_DISCOVERY_SHUFFLE=1
# MKSAP_YEAR_START=24
# MKSAP_YEAR_END=25
# MKSAP_CONCURRENCY=5

# Validation (optional)
# MKSAP_QUARANTINE_INVALID=1

# ============================================
# Phase 2: Statement Generator (Multi-Provider LLM)
# ============================================

# Provider Selection (optional, default: anthropic)
# Options: anthropic, claude-code, gemini, codex
# LLM_PROVIDER=anthropic

# ---- Anthropic Provider (API-based) ----
# Required if using LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=your_api_key_here

# ---- Claude Code Provider (CLI-based, uses existing subscription) ----
# Optional: Override CLI path if needed
# CLAUDE_CLI_PATH=claude

# ---- Gemini Provider (CLI-based, uses existing subscription) ----
# Optional: Override model and CLI path
# GEMINI_MODEL=gemini-pro
# GEMINI_CLI_PATH=gemini

# ---- Codex/OpenAI Provider (CLI-based, uses existing subscription) ----
# Optional: Override model and CLI path
# OPENAI_MODEL=gpt-4
# OPENAI_CLI_PATH=openai

# ---- Shared Settings (All Providers) ----
# Optional: Override defaults for LLM processing
# CLAUDE_MODEL=claude-sonnet-4-20250514  # For anthropic/claude-code
# TEMPERATURE=0.2
# MAX_TOKENS=4096
# BATCH_SIZE=10
# MAX_RETRIES=3
# LOG_LEVEL=INFO
